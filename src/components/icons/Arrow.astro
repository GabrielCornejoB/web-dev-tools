---
interface Props {
  direction: "left" | "right";
}

const { direction } = Astro.props;
---

{
  direction === "right" ? (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      class="inline-block h-12 w-12 stroke-base-content transition-colors"
    >
      <path
        d="M8 4L16 12L8 20"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg>
  ) : (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      class="inline-block h-12 w-12 stroke-base-content transition-colors"
    >
      <path
        d="M16 4L8 12L16 20"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      />
    </svg>
  )
}
